# Calcular el área de un rectangulo

largo = float(input("Entra el largo del rectangulo: "))
ancho = float(input("Entra el ancho del rectangulo: "))

area = largo*ancho

print(f"El área del rectangulo de largo {largo} y ancho {ancho} es {area}")